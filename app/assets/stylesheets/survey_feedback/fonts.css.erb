# ! This does not work! For unknown reasons this does not load the font file
@font-face {
  font-family: 'Lato-Regular';
  font-style: normal;
  font-weight: normal;
  src: url('<%= Rails.root.join('app', 'assets', 'fonts', 'Lato-Regular.ttf') %>') format('truetype');
}